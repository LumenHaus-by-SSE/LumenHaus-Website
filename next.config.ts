import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['www.pexels.com', "via.placeholder.com", "images.pexels.com", "videos.pexels.com", "upload.wikimedia.org"],
    },
};

export default nextConfig;